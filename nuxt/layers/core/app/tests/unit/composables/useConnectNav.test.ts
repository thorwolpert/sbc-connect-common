import { describe, expect, it } from 'vitest'
// import { mockNuxtImport } from '@nuxt/test-utils/runtime'

// will test once account store options are in here
describe('useConnectNav', () => {
  it('does stuff', () => {
    expect(1).toBeTruthy()
  })
})
